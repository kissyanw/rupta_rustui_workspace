# Test Programs

Place focused Lite DSL test programs here. Prefer small cases that isolate one cast/downcast pattern at a time, such as class inheritance, interface implementation, mixin usage, and must-success or must-fail downcasts.

## Indexes

- `lite_class_dsl_entries/`: audited candidate rcpta entry functions from the current `lite_class_dsl` tests, with priority/category tags for selecting focused cast-erasure programs.
